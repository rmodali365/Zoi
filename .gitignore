node_modules/
.expo/
dist/
web-build/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env.local
.env.development
.env.production
.env.*.local
.claude/settings.local.json

# Database dumps taken before a production migration. These contain real user
# data (phone numbers, every experience and trip) — never commit one.
# Extension-agnostic on purpose: the first one written was .sql.json, which an
# extension-anchored pattern silently failed to match.
prod-backup*
*-backup-2*
supabase/.temp/

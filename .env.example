EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Google Places key is NOT a client env var. It's stored server-side as a secret on the
# `places` Edge Function: supabase secrets set GOOGLE_PLACES_API_KEY=... --project-ref <ref>
